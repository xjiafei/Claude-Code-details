# Claude Code 源码深度分析 — 02 上下文工程（Context Engineering）

---

## System Prompt 的构建流程

Claude Code 的 system prompt 由多层动态内容拼接而成：

```
最终 System Prompt = 
  [静态基础指令]              ← 工具描述、行为规则、安全限制
+ [userContext.claudeMd]      ← CLAUDE.md 内容（项目+用户级）
+ [userContext.currentDate]   ← 当前日期（"Today's date is..."）
+ [systemContext.gitStatus]   ← git branch/status/log/user
+ [systemContext.cacheBreaker]← 调试用缓存破坏符（仅 ant 内部）
+ [appendSystemPrompt]        ← 用户自定义追加内容
```

### 核心实现（context.ts）

```typescript
// getUserContext() — 每次会话初始化时构建，之后 memoize 缓存
export const getUserContext = memoize(async () => {
  const claudeMd = getClaudeMds(filterInjectedMemoryFiles(await getMemoryFiles()))
  return {
    ...(claudeMd && { claudeMd }),
    currentDate: `Today's date is ${getLocalISODate()}.`,
  }
})

// getSystemContext() — 同样 memoize
export const getSystemContext = memoize(async () => {
  const gitStatus = await getGitStatus()  // 并行执行 5 个 git 命令
  return { ...(gitStatus && { gitStatus }) }
})
```

**精妙之处**：`memoize` 确保 git status 和 CLAUDE.md **只在会话开始时执行一次**，避免每次 API 调用都重新读取文件系统。注释中明确写道："This is a snapshot in time, and will not update during the conversation."

---

## CLAUDE.md 发现与注入机制

```typescript
// utils/claudemd.ts 中的文件发现逻辑（推断）
getClaudeMds(getMemoryFiles())
```

CLAUDE.md 的发现遵循**层级优先级**：
1. `~/.claude/CLAUDE.md` — 用户级（全局）
2. `<project-root>/CLAUDE.md` — 项目级
3. `<subdir>/CLAUDE.md` — 子目录级（nested memory）

**动态注入机制**（ToolUseContext）：
```typescript
nestedMemoryAttachmentTriggers?: Set<string>
loadedNestedMemoryPaths?: Set<string>  // 去重，避免重复注入
```

当 Claude 进入某个子目录时，该目录的 CLAUDE.md 会作为 `nested_memory` attachment **动态注入**到当前对话中，而不是预先全部加载。这是一个懒加载策略，避免大型 monorepo 中 CLAUDE.md 爆炸性增长。

---

## 上下文压缩的 6 层策略

这是 Claude Code 最复杂的工程部分，层层叠加：

```
策略1: Tool Result Budget (applyToolResultBudget)
  → 单条工具结果超过 maxResultSizeChars 时，截断并存磁盘
  → Claude 收到 "Preview + 文件路径" 而非完整内容

策略2: History Snip (HISTORY_SNIP feature flag)
  → 删除历史中的"片段"（非保护尾部）
  → snipTokensFreed 传给 autocompact 调整阈值

策略3: Microcompact (microCompact / apiMicrocompact)
  → 对旧工具调用进行微摘要
  → 两种实现：纯客户端 vs API 辅助（CACHED_MICROCOMPACT）

策略4: Context Collapse (CONTEXT_COLLAPSE feature flag)
  → 将历史消息"折叠"进 summary store（不删除，投影视图）
  → 收到 413 时优先尝试 drain 已暂存的折叠

策略5: Auto Compact (autoCompact)
  → 主动压缩：token 数超过阈值时，用 Claude 生成摘要
  → 阈值 = contextWindow - 20k(输出预留) - 13k(缓冲)
  → 电路断路器：连续失败 3 次停止重试

策略6: Reactive Compact (REACTIVE_COMPACT feature flag)
  → 被动压缩：收到 413 (prompt_too_long) 后触发
  → hasAttemptedReactiveCompact 标志防死循环
```

### 压缩触发顺序（query.ts 中的执行顺序）

```
每次 API 调用前:
  applyToolResultBudget → snip → microcompact → contextCollapse → autocompact

收到 413 响应后:
  contextCollapse.drain → reactiveCompact → 若均失败则 surface error
```

### autoCompact 阈值计算

```typescript
// autoCompact.ts
const MAX_OUTPUT_TOKENS_FOR_SUMMARY = 20_000  // p99.99 摘要输出
export const AUTOCOMPACT_BUFFER_TOKENS = 13_000  // 安全缓冲

getAutoCompactThreshold(model) = 
  getContextWindowForModel(model) 
  - min(getMaxOutputTokensForModel(model), 20_000)
  - 13_000
```

对于 200k context 的 claude-sonnet，阈值约为 **167,000 tokens**。

---

## 对话历史的数据结构与持久化

### 消息类型（types/message.ts）
```typescript
type Message = 
  | UserMessage        // 用户输入 + tool_result
  | AssistantMessage   // LLM 响应（可含 tool_use blocks）
  | AttachmentMessage  // 附件（文件变更、hooks、memory 等）
  | SystemMessage      // 系统通知（不发给 API）
  | ProgressMessage    // 工具执行进度（UI only）
  | TombstoneMessage   // 标记孤儿消息（fallback 时清除）
  | ToolUseSummaryMessage // Haiku 生成的工具摘要
```

### Compact Boundary 机制
```typescript
// query.ts:365
let messagesForQuery = [...getMessagesAfterCompactBoundary(messages)]
```
每次 compact 后插入一个"边界"消息，之后只发边界后的消息给 API，边界前的历史变成摘要。这保证了：
1. API 只看到在 context window 内的历史
2. 原始历史在 REPL UI 中仍可完整查看

### 跨会话持久化
- **REPL 输入历史**：`history.ts` 中，用 append-only 的 JSON Lines 写入 `~/.claude/projects/<hash>/history`
- **完整会话**：`assistant/sessionHistory.ts` 存储完整对话，支持 `/resume`
- **粘贴内容**：大型粘贴内容存入 `pasteStore`，消息中只保留 `[Pasted text #1 +10 lines]` 引用

---

## 关键设计决策 #2：上下文是"工程问题"而非"提示问题"

传统工具认为 "写好 prompt" 就足够，Claude Code 的做法是：
- 在 **6 个层次** 主动管理 token 预算
- 把 CLAUDE.md 当做**一等公民**，懒加载、去重、层级优先
- git 状态作为**不可变快照**注入（因为 git 操作可能慢且不稳定）
- 工具大结果**落盘**，给 Claude 一个指针而非完整内容

这反映了一个深刻认识：**LLM 的效果上限由上下文质量决定，而上下文质量是工程问题**。

---

## Memory Prefetch（记忆预取）

```typescript
// query.ts:301
using pendingMemoryPrefetch = startRelevantMemoryPrefetch(
  state.messages,
  state.toolUseContext,
)
```

在每轮循环开始时，**异步预取**与当前对话相关的记忆文件，在工具执行（2-30秒）期间完成，然后消费：
```typescript
// query.ts:1601
if (pendingMemoryPrefetch.settledAt !== null && 
    pendingMemoryPrefetch.consumedOnIteration === -1) {
  const memoryAttachments = filterDuplicateMemoryAttachments(...)
  // 注入到 toolResults，发给下一轮 API
}
```

`using` 关键字（TypeScript Explicit Resource Management）确保预取在 generator 退出时正确清理，无论是正常退出还是异常。这是对新语言特性的巧妙应用。
