# Claude Code 源码深度分析 — 03 工具系统设计

---

## Tool 接口设计（Tool.ts）

Claude Code 的工具系统是整个产品的核心差异化能力。每个工具必须实现一个极其丰富的接口：

```typescript
type Tool<Input, Output, P extends ToolProgressData> = {
  // === 核心执行 ===
  name: string
  call(args, context, canUseTool, parentMessage, onProgress?): Promise<ToolResult<Output>>
  inputSchema: Input          // Zod schema — 双重用途：LLM 描述 + 运行时验证
  
  // === 权限与安全 ===
  checkPermissions(input, context): Promise<PermissionResult>
  validateInput?(input, context): Promise<ValidationResult>
  isReadOnly(input): boolean      // 影响并发调度
  isConcurrencySafe(input): boolean  // 是否可与其他工具并发
  isDestructive?(input): boolean  // 影响用户确认逻辑
  
  // === LLM 接口 ===
  prompt(options): Promise<string>                    // 工具的自然语言描述
  description(input, options): Promise<string>        // 当前调用的描述
  mapToolResultToToolResultBlockParam(content, id)    // 序列化为 API 格式
  
  // === UI 渲染（React/Ink）===
  renderToolUseMessage(input, options): React.ReactNode
  renderToolResultMessage?(content, progress, options): React.ReactNode
  renderToolUseProgressMessage?(progress, options): React.ReactNode
  renderToolUseRejectedMessage?(input, options): React.ReactNode
  
  // === 元数据 ===
  maxResultSizeChars: number    // 超出则持久化到磁盘
  shouldDefer?: boolean         // 是否延迟加载（需先用 ToolSearch）
  alwaysLoad?: boolean          // 始终在 prompt 中出现
  isMcp?: boolean               // MCP 工具标志
  backfillObservableInput?(input): void  // 填充派生字段（不影响 prompt cache）
  toAutoClassifierInput(input): unknown  // 安全分类器输入
  
  // === 辅助 ===
  aliases?: string[]            // 向后兼容的别名
  searchHint?: string           // ToolSearch 关键词提示
  interruptBehavior?(): 'cancel' | 'block'  // 用户中断时的行为
}
```

**精妙之处**：同一个 `inputSchema`（Zod）既用于生成发给 LLM 的 JSON Schema（工具描述），也用于运行时验证 LLM 返回的参数。一份定义，两种用途，零重复。

### `buildTool()` 工厂函数

```typescript
// Tool.ts:783
export function buildTool<D extends AnyToolDef>(def: D): BuiltTool<D> {
  return {
    ...TOOL_DEFAULTS,  // 安全默认值（fail-closed）
    userFacingName: () => def.name,
    ...def,
  } as BuiltTool<D>
}

const TOOL_DEFAULTS = {
  isEnabled: () => true,
  isConcurrencySafe: () => false,   // 默认不安全（保守）
  isReadOnly: () => false,          // 默认写操作（保守）
  isDestructive: () => false,
  checkPermissions: () => Promise.resolve({ behavior: 'allow', updatedInput }),
  toAutoClassifierInput: () => '',  // 默认跳过安全分类（安全工具必须 override）
}
```

**默认值设计原则：fail-closed**
- 默认不并发（串行执行）→ 保证正确性
- 默认非只读（需要权限）→ 保证安全性
- 安全分类器默认跳过 → 只有 Bash 等高风险工具才参与分类

---

## 内置工具清单与核心实现

| 工具 | 描述 | 关键特性 |
|------|------|---------|
| **BashTool** | shell 命令执行 | 最复杂：安全检查、沙箱、destructive 检测 |
| **FileReadTool** | 读取文件 | maxResultSizeChars=∞（不落盘，已自限制）|
| **FileEditTool** | 精确字符串替换 | 要求 unique old_string，保护 prompt cache |
| **FileWriteTool** | 覆盖/创建文件 | 必须先 Read（工具规则强制） |
| **GrepTool** | 正则内容搜索 | isConcurrencySafe=true，可并发 |
| **GlobTool** | 文件路径匹配 | isConcurrencySafe=true，可并发 |
| **AgentTool** | 启动子 Agent | 最关键：递归 query() 调用 |
| **EnterPlanModeTool** | 进入计划模式 | 切换 permissionMode='plan' |
| **EnterWorktreeTool** | 创建 git worktree | 沙箱隔离 |
| **NotebookEditTool** | Jupyter 编辑 | 专门的 .ipynb 处理 |
| **MCPTool** | 外部 MCP 工具代理 | 动态工具 |
| **TodoWriteTool** | 任务列表 | 不渲染到 transcript，只更新 UI panel |
| **WebSearchTool** | 网页搜索 | 需要权限 |

### BashTool 的安全架构（bashSecurity.ts）

BashTool 是整个系统最复杂的工具，有独立的安全层：

```
BashTool 执行流程:
  1. bashSecurity.ts  → 静态安全分析（黑名单命令、路径注入检测）
  2. modeValidation   → 模式检查（计划模式不允许写操作）
  3. pathValidation   → 路径越界检查
  4. readOnlyValidation → 只读模式检查
  5. sedValidation    → sed 命令特殊验证
  6. checkPermissions → 规则匹配（alwaysAllow/alwaysDeny）
  7. 用户确认 UI      → 展示 diff/命令，等待确认
  8. shouldUseSandbox → 决定是否沙箱执行
  9. 实际执行 spawn()
  10. destructiveCommandWarning → 事后告警
```

**Auto-mode Classifier**：在 `bypassPermissions` 模式下，Bash 命令通过 `toAutoClassifierInput()` 送入安全分类器（yoloClassifier），用 Claude 本身来判断操作是否安全。

---

## 工具执行管道：从 LLM 返回到落地执行

```
LLM 返回 tool_use block
    │
    ▼
StreamingToolExecutor.addTool()  ← 流式阶段立即开始
    │
    ▼
toolExecution.ts: runToolUse()
    │
    ├── 1. findToolByName()          ← 查找工具定义
    ├── 2. validateInput()           ← Zod schema 验证
    ├── 3. startToolSpan()           ← OpenTelemetry 追踪
    ├── 4. executePreToolHooks()     ← 用户自定义 pre-hooks
    │       ├── 若 hook 返回 block → 展示给用户，可能拒绝执行
    │       └── 若 hook 修改 input → 用修改后的 input 继续
    ├── 5. checkPermissions()        ← 权限检查
    │       ├── checkRuleBasedPermissions() ← alwaysAllow/alwaysDeny 规则
    │       ├── tool.checkPermissions()    ← 工具专属逻辑
    │       └── 若需要 → showPermissionDialog() ← 等待用户
    ├── 6. tool.call()               ← 实际执行
    │       └── onProgress(data) → yield ProgressMessage → UI 实时更新
    ├── 7. executePostToolHooks()    ← 用户自定义 post-hooks
    │       └── 可修改工具输出（toolOutput）
    ├── 8. mapToolResultToToolResultBlockParam() ← 序列化
    ├── 9. applyToolResultBudget()   ← 大结果落盘
    └── 10. createUserMessage({tool_result: ...}) ← 返回给 LLM
```

---

## 权限模型详解

### 权限模式（PermissionMode）

```typescript
// types/permissions.ts
export const EXTERNAL_PERMISSION_MODES = [
  'acceptEdits',      // 自动接受文件编辑，其他需确认
  'bypassPermissions',// 跳过所有权限检查（危险！）
  'default',          // 标准确认模式
  'dontAsk',          // 自动接受一切（等同于 --dangerously-skip-permissions）
  'plan',             // 只读模式，不允许写操作
]

// 内部模式（不暴露给用户）
'auto'    // 自动模式（配合 transcript classifier）
'bubble'  // 冒泡到父 context 的权限（子 Agent 用）
```

### 规则系统

权限规则支持细粒度的 glob 匹配：
```json
// settings.json
{
  "permissions": {
    "allow": ["Bash(git *)", "Read(/src/**)"],
    "deny": ["Bash(rm -rf *)", "Write(/etc/**)"]
  }
}
```

规则来源有优先级：
```
policySettings (最高) 
> flagSettings (--permission-mode CLI)
> localSettings (.claude/settings.local.json)
> projectSettings (.claude/settings.json)
> userSettings (~/.claude/settings.json，最低)
```

### 危险操作识别

```typescript
// Tool 接口
isDestructive?(input): boolean

// BashTool 的实现（推断）
// 检测 rm -rf, git reset --hard, DROP TABLE 等
// 文件写入工具在覆盖已存在文件时标记为 destructive
```

---

## MCP（Model Context Protocol）集成

MCP 工具通过 `MCPTool` 代理，在运行时动态注册：

```
MCP Server 连接
    │
    ▼
services/mcp/
├── 发现工具列表（tools/list）
├── 转换为内部 Tool 格式
│   ├── name: "mcp__serverName__toolName"（带前缀，避免冲突）
│   ├── inputSchema: 从 MCP JSON Schema 转换
│   └── isMcp: true
└── 注册到 toolUseContext.options.tools

query.ts 中:
  mcpTools: appState.mcp.tools  ← 注入到每次 API 调用
  hasPendingMcpServers: ...      ← 通知模型等待连接
  
// 每轮循环结束刷新（新连接的 MCP server 立即可用）
if (updatedToolUseContext.options.refreshTools) {
  const refreshedTools = updatedToolUseContext.options.refreshTools()
}
```

---

## 工具系统的反直觉设计

### 1. Tool 同时包含 UI 渲染代码

工具定义（`Tool.ts`）直接包含 `renderToolUseMessage()`, `renderToolResultMessage()` 等 React 渲染方法。这违反了通常的关注点分离原则，但带来了：
- 工具定义即文档：一个文件包含完整信息
- 渲染与工具逻辑强耦合，永远不会不同步
- 新增工具只需一个文件

### 2. maxResultSizeChars 是工具的属性

不同工具有不同的结果大小限制：
- `FileRead`：`Infinity`（已自限制，不能落盘——否则 Claude 会无限 Read 生成的文件）
- `Bash`：较大（命令输出可能很长）
- `Grep`：中等

这种工具自声明结果预算的设计，比全局阈值更精细。

### 3. `backfillObservableInput` 保护 prompt cache

```typescript
// query.ts:762
if (tool?.backfillObservableInput) {
  const inputCopy = { ...originalInput }
  tool.backfillObservableInput(inputCopy)
  // 只有 "添加了新字段"（非覆盖）时才 yield 克隆
  // 原始 input 永远不变 → 保护 prompt cache 字节对齐
}
```

这解决了一个精妙的 prompt caching 问题：某些工具需要在 observable 字段中展示派生信息（如展开的绝对路径），但修改 input 会破坏 API 的字节级 prompt cache 匹配。解决方案是维护两份拷贝：原始 input 发给 API，扩充版 input 展示给观察者。
